name: Playwright Tests with Allure + GitHub Pages

# 📦 Ce workflow s'exécute :
# - à chaque push ou pull request sur la branche `main`
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # 🔧 JOB PRINCIPAL : Exécution des tests Playwright + génération du rapport Allure
  test:
    runs-on: ubuntu-latest

    # ✅ Utilise une image Docker Playwright existante correspondant à la version 1.50.0
    container:
      image: mcr.microsoft.com/playwright:v1.50.0-jammy

    steps:
      # 1️⃣ Récupère le code source du repo
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Installe Java (nécessaire pour faire fonctionner la CLI Allure)
      - name: Install Java (for Allure)
        run: |
          apt-get update -y
          apt-get install -y default-jdk
          echo "JAVA_HOME=$(dirname $(dirname $(readlink -f $(which java))))" >> $GITHUB_ENV

      # 3️⃣ Installe les dépendances du projet + Allure CLI
      - name: Install dependencies
        run: |
          npm ci
          npm install -g allure-commandline

      # ⚠️ 3️⃣.5️⃣ Nettoie les anciens résultats Allure
      - name: Cleanup previous Allure results
        run: rm -rf allure-results

      # 4️⃣ Lance les tests Playwright
      - name: Run Playwright tests
        run: |
          npx playwright test --reporter=line
        continue-on-error: true

      # 5️⃣ Génère un rapport Allure dans un dossier horodaté : ./public/history/yyyy-mm-dd-<sha>/report
      - name: Generate timestamped Allure report
        if: always()
        run: |
          DATE=$(date +'%Y-%m-%d')
          SHA=${GITHUB_SHA::7}
          REPORT_DIR="./public/history/${DATE}-${SHA}/report"
          mkdir -p "$REPORT_DIR"
          if [ -d "allure-results" ]; then
            allure generate allure-results --clean -o "$REPORT_DIR"
          else
            echo "⚠️ Aucun dossier 'allure-results' trouvé."
            echo "<h1>⚠️ Aucun rapport disponible</h1>" > "$REPORT_DIR/index.html"
          fi

      # 6️⃣ Génère dynamiquement un index.html premium dans ./public/
      - name: Generate summary index.html
        if: always()
        env:
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          GITHUB_WORKFLOW: ${{ github.workflow }}
        run: npm run report:index

      # 7️⃣ Copie le dernier rapport dans ./public/allure-reports/latest
      - name: Create latest alias
        if: always()
        run: |
          DATE=$(date +'%Y-%m-%d')
          SHA=${GITHUB_SHA::7}
          cp -r "./public/history/${DATE}-${SHA}/report" ./public/allure-reports/latest

      # 8️⃣ Sauvegarde le dossier ./public pour déploiement
      - name: Upload public artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: public
          path: ./public

  # 🌍 JOB DE DÉPLOIEMENT : déploie les rapports HTML sur GitHub Pages
  deploy:
    needs: test
    runs-on: ubuntu-latest

    # ⛔ On ne déploie que si on est sur la branche main
    if: github.ref == 'refs/heads/main'

    steps:
      # 1️⃣ Récupère le code du dépôt
      - name: Checkout
        uses: actions/checkout@v3

      # 2️⃣ Récupère les fichiers générés dans le dossier ./public
      - name: Download public artifact
        uses: actions/download-artifact@v4
        with:
          name: public
          path: ./public

      # 3️⃣ Vérifie que le dernier rapport existe
      - name: Check if latest report exists
        run: |
          if [ ! -f "./public/allure-reports/latest/index.html" ]; then
            echo "❌ Dernier rapport Allure non trouvé."
            exit 1
          fi

      # 4️⃣ Déploie le contenu du dossier ./public sur la branche gh-pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages

      # 5️⃣ Ajoute les liens directs dans le résumé GitHub Actions
      - name: Add landing page links
        run: |
          REPO_NAME=${{ github.event.repository.name }}
          REPO_OWNER=${{ github.repository_owner }}
          BASE_URL="https://${REPO_OWNER}.github.io/${REPO_NAME}"

          echo "🏠 [Page d’accueil]($BASE_URL/index.html)" >> $GITHUB_STEP_SUMMARY
          echo "📊 [Dernier rapport Allure (latest)]($BASE_URL/allure-reports/latest/index.html)" >> $GITHUB_STEP_SUMMARY
          echo "📚 [Historique des rapports]($BASE_URL/history/)" >> $GITHUB_STEP_SUMMARY
